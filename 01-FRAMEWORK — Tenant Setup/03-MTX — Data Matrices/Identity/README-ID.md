# Identity MTX Samples

Small reference matrices for identity-related runtime inputs.

These examples align with the current AUT baseline fields for users, groups, permissions, and license input contracts. License rows are input examples only; global AUT scripts load license data but do not assign licenses.
